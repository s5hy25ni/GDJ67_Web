$(function(){
	var fileForm = /(.*?)\.(?!exe)[^\.]*$/i;
	$("#file").on("change", function(){
		if(!$(this).val().match(fileForm)){
			alert(".exe 파일은 업로드 할 수 없습니다.");
			$(this).val('');
			return;
		}
	});
});